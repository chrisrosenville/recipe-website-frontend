FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000

# Force legacy Webpack dev server instead of Turbopack
ENV NEXT_DEV_SERVER=legacy

CMD ["npm", "run", "dev"]
